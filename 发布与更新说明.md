# 发布与更新说明（润野灌溉设计工具 · 整合在线地图版）

## 线上网址
https://zhanglihui2026.github.io/runye-irrigation/

GitHub 仓库：https://github.com/zhanglihui2026/runye-irrigation

---

## 一、本地仓库位置
`C:\Users\AHS\runye-irrigation\`

里面有：9 个网页 + hero-driptape.jpg + README.md + 发布与更新说明.md + 推送更新.bat

> 开发版在 `C:\Users\AHS\WorkBuddy\2026-08-12-15-50-50\deploy-runye\`（含备份和测试脚本，未发布）。
> 若你在开发版改了东西，记得把改动的文件复制到本目录再推送。

---

## 二、首次发布（已完成，仅备忘）
用 GitHub Desktop 登录 → Add Local Repository 选本目录 → Publish repository（Public）→
仓库 Settings → Pages → Branch 选 `master`、folder 选 `/ (root)` → Save。

⚠️ 注意：本仓库默认分支是 **`master`**（不是 main），Pages 的 Branch 要选 `master`。
Pages 只需配置一次，之后每次推送自动重新部署。

---

## 三、图文：当前待推送内容如何用 GitHub Desktop 推上去

> 本地已有 2 个提交还没推到网上（新增「一键推送脚本」+「发布说明」、以及修正脚本分支名）。
> 用 GitHub Desktop 推一次即可，凭据已存、不会卡。

### 步骤 1：打开 GitHub Desktop
- 开始菜单 / 桌面双击 **GitHub Desktop**（已登录 `zhanglihui2026` 账号）

### 步骤 2：确认打开的是本仓库
- 顶部中间下拉应显示 **runye-irrigation**
- 若显示别的仓库：菜单 **File → Add Local Repository…** → 选 `C:\Users\AHS\runye-irrigation` → Add

### 步骤 3：看到待提交改动
- 中间 **Changes** 列表应出现：
  - `推送更新.bat`（新增）
  - `发布与更新说明.md`（新增/更新）
- 左下角 **Summary** 输入框填：`新增一键推送脚本与发布说明`

### 步骤 4：提交到本地
- 点蓝色按钮 **Commit to master**（注意是 master，不是 main）

### 步骤 5：推送到GitHub
- 点窗口**顶部**的 **Push origin**（蓝色按钮，在 Repository 名右侧）
- 进度条走完即成功

### 步骤 6：验证
- 浏览器打开 https://github.com/zhanglihui2026/runye-irrigation
- 文件列表里能看到 `推送更新.bat` 和 `发布与更新说明.md` 即成功

---

## 四、图文：以后每次更新网页

### 方式 A（推荐，图形，凭据现成不会卡）
1. 修改 `C:\Users\AHS\runye-irrigation\` 里的网页文件（如 index.html）
2. 打开 GitHub Desktop → 顶部确认仓库是 runye-irrigation
3. 中间 **Changes** 列出改动文件 → 左下 **Summary** 填更新说明 → 点 **Commit to master**
4. 点顶部 **Push origin** → 完成
5. 约 30 秒~2 分钟后刷新网址即见新版本

### 方式 B（一键 bat，走 Desktop 已存凭据）
1. 改完文件后，直接**双击** `C:\Users\AHS\runye-irrigation\推送更新.bat`
2. 按提示输入更新说明（回车用默认）
3. 脚本自动提交并推送（推到 `master` 分支）
4. 若提示推送失败（如凭据失效），改用方式 A 在 Desktop 里推一次即可

---

## 五、一键脚本 `推送更新.bat` 说明
- 自动检测 remote、列出改动、提交（留空说明则用日期默认）、推送到 `master`
- 若推送失败会给出常见原因（远程有改动需先 pull / 凭据失效需重登 / 网络）
- 推送成功会打印线上网址

---

## 六、注意事项
- 地图页与"地图回传"功能依赖联网加载高德卫星瓦片 + Leaflet CDN，并通过 localStorage 跨页传参，发布后照常可用。
- 务必用 **Public** 仓库，私有仓库 GitHub Pages 需付费。
- 用户名是 `zhanglihui2026`（非邮箱），网址与 remote 均以此为准。
- 默认分支是 `master`；如果哪天在 GitHub 网页改了默认分支，脚本和 Desktop 推送的分支名都要同步改。
