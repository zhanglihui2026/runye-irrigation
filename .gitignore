# 系统/编辑器临时文件
.DS_Store
Thumbs.db
*.log
*.bak
*~

# 自动化运行数据（不发布）
.workbuddy/

# 本地发布脚本（不发布）
*.bat
__pycache__/
*.pyc

# 本地验收 / 诊断临时产物（下划线开头：备份、截图、审计输出等，不发布到 GitHub Pages）
_*/

# 云端诊断/CloudBase 本地文件（不发布到 GitHub Pages）
CLOUDBASE_SETUP.md
cloud-diag.html
cloud-sync.js
cloudbaserc.json
cloud-function/
cloud-functions/
functions/

# 本地验收脚本与勘察报告（不发布到 GitHub Pages）
verify_*.js
verify_*.cjs
audit_*.js
docs/

# 一次性调试残留（下划线开头的文件；目录由上面的 _*/ 覆盖）
# 注意：不能用 `_*` 通配——仓库里已有被跟踪的 _delcards.py
_out_*
_patch_*
_r_*
*.pre-*
*.post-*
*.rv_bak

# CDP 探针 DOM 快照目录与历史探针脚本（纯调试产物，不发布）
shots_*/
_bk*
_chk_*
_gchk*
_imgchk*
_gates_*
runye_v9*.cjs
runye_v100_merge.cjs

# 下划线开头的排查/校验文本快照（纯调试产物；.py 不在此列，_delcards.py 已被跟踪不受影响）
_*.txt

# v98~v105 时代历史调试探针/本地闸门器/单测（28 个，22:3x 用户桌面端 Changes 清单清净到 0；均无页面运行时依赖）
runye_v98*
runye_v101*
runye_v104*
runye_v105*
runye_gates.cjs
tests/
