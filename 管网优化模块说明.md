# 管网优化详细分析模块（pipe_optim）· 隔离开发说明

> 本模块为「润野灌溉网页灌溉设计工具」新增的**独立**管网优化分析模块，采用 Jaya 群智能算法，对三级管网做管径优化与多拓扑比选。**严格遵守隔离开发原则，不修改任何原有业务代码。**

---

## 一、模块文件清单（全部为新增文件）

| 文件 | 作用 | 是否改动原站 |
|------|------|--------------|
| `pipe_optim_ui.js` | 隔离 UI 模块：导航注入、弹窗、只读数据提取、三拓扑生成、渲染、导出 | 否（运行时注入） |
| `irr_optim.js` | Jaya 群智能优化引擎（独立命名空间 `window.IrrOptim`） | 否 |
| `pipe_optim.css` | 弹窗/表格/图表隔离样式（`po-` 前缀，运行时注入 `<link>`） | 否 |
| `管网优化模块说明.md` | 本文档 | 否 |
| `index.html` | **仅新增一行** `<script src="pipe_optim_ui.js">`（见第五节） | 仅 +1 行入口 |

原有文件（`runye-nav.js` / `runye-nav.css` / `三级系统图.html` / `二级系统图.html` / 各业务模块 JS/CSS/HTML）**均未被修改、删除或重构**。原有全局变量、函数、DOM 结构保持不变。

---

## 二、隔离原则（强制性）

1. **独立命名空间**：UI 逻辑封装于 `window.PipeOptim`，算法封装于 `window.IrrOptim`，与原站任何全局变量/函数零交叉引用。
2. **只读数据**：读取原站数据一律 **深拷贝**（`JSON.parse(JSON.stringify())`），与原站对象**零引用共享**；本模块**绝不写回**任何原站数据（不修改 `tlDiagramData`、`measuredPolygon`、`ppState`、`RunyeBridge` 等）。
3. **崩溃隔离**：UI 全程 `try/catch` 包裹；本模块任何异常只影响自身，**绝不**导致原站功能失灵。
4. **零侵入导航**：导航按钮追加到 `.fn-inner[data-ry-fnnav]` 末尾，且**不带** `data-ry-navitem` 属性 —— 原 `runye-nav.js` 的 `render()` 只清理带该属性的节点，因此本按钮不会被移除，也不需要修改 `runye-nav.js` 的 `ITEMS` 清单。
5. **样式隔离**：所有类名以 `po-` 前缀，绝不覆盖原站任何 CSS 规则。

---

## 三、只读数据源清单（明确禁止写回）

| 数据 | 来源（只读） | 用途 |
|------|--------------|------|
| 全网拓扑快照 | `window.tlDiagramData`（深拷贝；含 `mainPipes`/`branchPipes`/`frontPipe`/`sourcePos`/`plot`/`poly`/`meta.flowModel`） | 管段几何、流量模型、水源位置 |
| 地块多边形 | `window.measuredPolygon` 或 `window.ppGetPolyPts()`（回退 `tlDiagramData.poly`） | 地块轮廓（展示用） |
| 二级页状态桥 | `window.RunyeBridge.state` / `getZoneCuts()` / `getPlanDims()`（只读） | 备用分区网格（当前版本主要用 `tlDiagramData`） |

> ⚠️ **写回禁令**：模块内部任何函数都不得对以上对象执行赋值、`.push`、属性改写等操作。`exportJSON()` 仅把**优化结果**下载为本地 JSON 文件，由用户人工核对后手动回填，**模块绝不自动覆盖**原管网/地块参数。

---

## 四、功能与交互

- 顶部导航新增菜单项 **【管网优化详细分析】**（置于导航末尾）。点击打开独立弹窗。
- 弹窗内可配置：种群规模、迭代次数、最不利压力下限、流速上限、水源扬程、Hazen-Williams C。
- 点击 **读取并分析**：
  1. 只读深拷贝当前三级管线图数据；
  2. 构建管网图（节点 + 管段，含层级与长度）；
  3. 生成三套拓扑并分别用 Jaya 优化管径；
  4. 输出 **三方案对比表**（管材总长 / 总投资 / 单位管长造价 / 最不利压力 / 最大水头损失 / 最大流速 / 可行性），最优可行方案高亮；
  5. 输出 **最优方案逐管段推荐管径表** 与 **节点压力分布图**（低于下限者标红）。
- 点击 **导出最优方案 JSON**：下载分析结果（含逐管段 DN、流量、流速、水头损失、节点压力），**仅供人工核对回填**。

---

## 五、嵌入方式（原 index.html 唯一改动）

在 `runye-nav.js` 引用之后新增一行（已添加，带清除标记）：

```html
  <script src="runye-nav.js"></script>
  <!-- [NEW MODULE: pipe_optim] 入口：删除本行即卸载本隔离模块（不改动任何原有逻辑） -->
  <script src="pipe_optim_ui.js"></script>
```

> 本模块所需的 CSS `<link>` 与弹窗 DOM 均由 `pipe_optim_ui.js` 在运行时注入，**无需修改** `<head>` 或原导航结构。

---

## 六、一键卸载（恢复原站）

1. 删除 `index.html` 中上述带 `[NEW MODULE: pipe_optim]` 标记的两行（注释 + `<script>`）；
2. 删除新增文件：`pipe_optim_ui.js`、`irr_optim.js`、`pipe_optim.css`、`管网优化模块说明.md`。

完成后原站导航、功能、数据与原先完全一致，无任何残留。

---

## 七、三套拓扑假设（明确标注，便于复核）

三套拓扑代表三种**布线/冗余策略**，由模块基于同一只读图自动派生；最终造价与压力为 Jaya 优化后的**实证结果**，并非预设排序——小口径并联有时反而比单根大口径更省料，请以对比表为准。

| 拓扑 | 布线/冗余假设 | 说明 |
|------|--------------|------|
| **梳型 comb** | 主管单管、满载（流量 = 总流量 `combinedFlow`） | 基准树状布置，管长最少 |
| **丰型 abundant** | 主管并行双管（几何 ×2），各承担总流量 1/2 | 冗余丰水布置；优化器可能将并联管径下调，造价与压力以实际输出为准 |
| **π型 pi** | 主管双端供水成环（各半流量）+ 闭合回流段 | 环路布置，靠双端供水降低水头损失 |

> 流量模型来自 `tlDiagramData.meta.flowModel`（`combinedFlow`/`branchFlow`，单位 m³/h，模块内换算为 m³/s）。主管按满载估算偏保守（实际树状下游会递减，但满载取值保证不低估损失）。
> 优化器对每套拓扑**独立**选取各管段管径以最小化造价；因此三者的"谁更省/谁压力更好"取决于具体几何与流量，对比表即给出答案。

---

## 八、优化模型与约束

- **决策变量**：每条管段的管径等级（离散，从管径目录 `DEFAULT_CATALOG` 中选取，SDR11 PE100，含内直径与占位单价）。
- **目标函数**：最小化管材总造价 `Σ 管长 × 单价`。
- **水头损失**：Hazen-Williams 公式 `h_f = 10.67·L·Q^1.852 / (C^1.852·d^4.87)`（PE 取 C=150；Q 为 m³/s，d 为内直径 m，L 为 m）。
- **节点压力**：以水源为根做生成树 BFS 累计水头损失；各节点压力 = 水源扬程 − 累计损失；最不利压力取最小值（对环路按最小损失路径估值，偏安全）。
- **约束**：最不利节点压力 ≥ 最不利压力下限 **且** 各管段流速 ≤ 流速上限。不可行解以惩罚项计入适应度，Jaya 自然淘汰。
- **算法**：Jaya（无参数群智能，Rao 2016）—— 每代个体同时向当前种群最优靠近、远离最差，离散管径经取整夹取；保留全程最优。

---

## 九、原生 wasm 接缝（opt-in，默认走 JS 引擎）

`irr_optim.js` 当前为**纯 JS 的 Jaya 实现**，算法语义与未来 wasm 二进制一致。若要替换为原生 `irr_optim.wasm`：

1. 将编译好的 `irr_optim.wasm` 放到同级目录；
2. 在页面（或控制台）设置 `window.PIPE_OPTIM_USE_WASM = true;`
3. 调用 `IrrOptim.loadWasm('irr_optim.wasm')`（返回 Promise）。

加载成功后 `optimize()` 自动改走 wasm 导出函数（导出签名需为 `(topologyJson, cfgJson) -> resultJson`，结果与 JS 版结构一致）。未提供 wasm 时引擎自动回退 JS，模块照常工作。

---

## 十、免责声明

本模块为**辅助比选工具**，基于只读快照与简化水力假设（生成树压力、主管满载、占位单价）。优化结果**不能直接替代**原「水力计算器」的正式计算，应用前务必用原工具复核管径、压力与造价，并以实际报价与现场条件为准。
